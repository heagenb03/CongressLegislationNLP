Baseline produced at commit 4b07ea1 on the unmodified tree.

Produced by `python scripts/modeling/extract_features.py` under .venv (Python
3.13.9). The congress_nlp package skeleton (pyproject.toml, six empty
__init__.py files, requirements.txt) was created before this run but
extract_features.py was byte-for-byte unmodified at the time -- `git diff` on it
was empty. The skeleton commit lands immediately after this file.

Any regeneration of features.csv must match the row counts and positive
counts below exactly. A mismatch means the ids.py merge changed
de-duplication -- investigate, do not accept a new number.

The `missing JSON` and `missing summary` counters are NOT part of the
acceptance check. Task 6 changes unrecognized legislation types from
"produce a nonexistent path, count as missing" to "skip, uncounted", so
`missing JSON` may move without indicating a problem.

======================================================================
  FEATURE EXTRACTION SUMMARY
======================================================================
  train :  848 bills | 558 pos / 290 neg (66% positive) | summary: 100% | strong kw: 35%
  val   :  222 bills | 179 pos / 43 neg (81% positive) | summary: 100% | strong kw: 40%
  test_legacy:  184 bills | 178 pos / 6 neg (97% positive) | summary: 67% | strong kw: 55%
  test  :  489 bills | 250 pos / 239 neg (51% positive) | summary: 33% | strong kw: 30%
----------------------------------------------------------------------
  NOTE: 'summary' is CRS coverage. The `text` field (title+summary)
  shrinks where it is low; `text_title_subjects` does not. Prefer the
  latter for training - see CLAUDE.md.
======================================================================

Total: 1743 rows.

Per-file processing counts from the same run:
  twl_coded_legislation_101_to_118.csv : 1216 labeled -> 1122 records
  intern_coded_119.csv                 :  489 labeled ->  489 records
  intern_coded_negatives_101_116.csv   :  140 labeled ->  140 records
